<% content_for :title, "Editing character" %>

<h1>Editing character</h1>

<%= render "form", character: @character %>

<div class="d-flex flex-wrap gap-2 mt-3">
  <%= link_to "Show this character", @character, class: "btn btn-outline-secondary" %>
  <%= link_to "Back to characters", characters_path, class: "btn btn-outline-secondary" %>
</div>
