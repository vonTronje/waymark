<% content_for :title, "Editing user" %>

<h1>Editing user</h1>

<%= render "form", user: @user %>

<div class="mt-3">
  <%= link_to "Show this user", @user, class: "btn btn-outline-secondary" %>
</div>
