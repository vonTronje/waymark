<% access = shareable.access_for(Current.user) %>
<% access = "admin" if access.blank? && Current.user&.admin? %>
<% if access.present? %>
  <p class="access-badge badge text-bg-secondary"><%= access %></p>
<% end %>
